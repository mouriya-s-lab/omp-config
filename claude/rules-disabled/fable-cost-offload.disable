# Fable 会话最小化 Fable 支出，执行卸载给 opus subagent

当前会话模型为 Fable（`claude-fable-5`）时，计费仅按 Fable 计，因此 Fable 本体的 token 支出要压到最低：凡可交给 subagent 的工作（代码检索、文件通读、调研、批量编辑、跑测试、写样板代码），一律通过 Agent tool 卸载，并显式设 `model: "opus"`；Fable 只消费结论。

## 边界：什么不许卸载

**设计与决策必须由 Fable 自己完成**：架构取舍、方案选择、接口/数据模型设计、计划制定与验收判断、对用户的最终结论。subagent 只做执行与信息收集，不做拍板；subagent 返回的"建议方案"只当输入，Fable 重新裁决。

## 派发与消费 subagent 的要求

- **调研指派必须详细**：prompt 写明背景、目标、要查的具体位置/命令、期望输出结构与判定标准，不许甩一句模糊任务。
- **默认 subagent 的调研不充分**：其结论按线索对待，不按事实。关键事实（会写进回复、决定方案、触发改动的）由 Fable 用最小成本抽查核验（点名读 `path:line`、跑单条命令），或派第二个 subagent 交叉验证；核不过就打回重查，不许直接转述。
- 多个独立任务并行派发，减少 Fable 空转轮次。

Why：Fable 每个 token 都计费且贵，opus subagent 输出便宜；但 subagent 质量参差，把决策或未核验事实交给它，省下的钱会以返工和错误结论加倍还回来。

providers:
  futureppo:
    baseUrl: https://ai.futureppo.top/v1
    api: openai-completions
    apiKey: FUTUREPPO_API_KEY
    discovery:
      type: proxy
    models:
      - id: glm-5-2
        reasoning: true
        thinking:
          mode: effort
          efforts: [minimal, low, medium, high, max]
          defaultLevel: medium
